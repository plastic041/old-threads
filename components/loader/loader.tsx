const Loader = () => {
  return (
    <div className="mt-40 flex w-full flex-grow items-center justify-center">
      <span className="animate-pulse text-teal-900">
        페이지를 불러오는 중이에요 \(￣︶￣*\))
      </span>
    </div>
  );
};
export default Loader;
